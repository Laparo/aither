# =============================================================================
# Aither — Hemera Academy API Integration
# =============================================================================
# Copy this file to .env and fill in the values.
# NEVER commit .env to version control.
# =============================================================================

# --- Hemera Academy API ---
HEMERA_API_BASE_URL=https://api.hemera.academy
HEMERA_API_KEY=your-api-key-here

# --- Clerk Authentication ---
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_...
CLERK_SECRET_KEY=sk_...

# --- SMTP Notifications ---
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=password
SMTP_FROM=aither@example.com
NOTIFY_EMAIL_TO=operator@example.com
NOTIFY_FAILURE_THRESHOLD=3

# --- Rollbar Error Monitoring ---
ROLLBAR_SERVER_TOKEN=your-rollbar-server-token
NEXT_PUBLIC_ROLLBAR_CLIENT_TOKEN=your-rollbar-client-token


# --- Rollbar MCP Server (read+write scope for AI tools) ---
# ROLLBAR_ACCESS_TOKEN grants read+write access. For most use-cases, prefer a read-only token (see Rollbar dashboard → Project Access Tokens → create read-only token).
# If you use AI tools or automation, set ROLLBAR_ACCESS_TOKEN_READONLY=your-rollbar-readonly-token and update your config to use it where possible.
ROLLBAR_ACCESS_TOKEN=your-rollbar-read-write-token
# ROLLBAR_ACCESS_TOKEN_READONLY=your-rollbar-readonly-token

# --- Rollbar Control Flags (1 = enabled/true, 0 = disabled/false) ---
# ┌─────────────────────────────────┬──────────┬──────────┐
# │ Flag                            │ Prod     │ Local    │
# ├─────────────────────────────────┼──────────┼──────────┤
# │ NEXT_PUBLIC_ROLLBAR_ENABLED     │ 1        │ 1        │
# │ ROLLBAR_ENABLED                 │ 1        │ 1        │
# │ NEXT_PUBLIC_DISABLE_ROLLBAR     │ 0        │ 0        │
# └─────────────────────────────────┴──────────┴──────────┘
# Set NEXT_PUBLIC_DISABLE_ROLLBAR=1 to kill-switch Rollbar in any environment.
# NEXT_PUBLIC_ROLLBAR_ENABLED=1
# ROLLBAR_ENABLED=1
# NEXT_PUBLIC_DISABLE_ROLLBAR=0

# --- Rollbar Sampling Rates (0.0–1.0, validated at startup) ---
# Controls what fraction of events are sent to Rollbar.
# Prod: keep error/critical at 1.0; reduce info/warn to save quota.
# Local: set all to 1.0 for full visibility during development.
# ┌──────────────────────────────┬──────────┬──────────┐
# │ Rate                         │ Prod     │ Local    │
# ├──────────────────────────────┼──────────┼──────────┤
# │ ROLLBAR_SAMPLE_RATE_ALL      │ 1        │ 1        │
# │ ROLLBAR_SAMPLE_RATE_INFO     │ 0.05     │ 1        │
# │ ROLLBAR_SAMPLE_RATE_WARN     │ 0.05     │ 1        │
# │ ROLLBAR_SAMPLE_RATE_ERROR    │ 1        │ 1        │
# │ ROLLBAR_SAMPLE_RATE_CRITICAL │ 1        │ 1        │
# └──────────────────────────────┴──────────┴──────────┘
# ROLLBAR_SAMPLE_RATE_ALL=1
# ROLLBAR_SAMPLE_RATE_INFO=0.05
# ROLLBAR_SAMPLE_RATE_WARN=0.05
# ROLLBAR_SAMPLE_RATE_ERROR=1
# ROLLBAR_SAMPLE_RATE_CRITICAL=1

# --- Privacy / PII (1 = allow PII, 0 = scrub all user-identifying data; defaults to 0/false) ---
# ⚠️  PRODUCTION: Keep at 0 unless you have explicit user consent (GDPR/DSGVO). PII logging is opt-in only.
# NEXT_PUBLIC_TELEMETRY_CONSENT=0
# ROLLBAR_ALLOW_PII=0

# --- E2E Testing ---
# Set to 1 to disable real Rollbar calls during E2E test runs. Accepts 0/1, true/false. Defaults to 0/false.
# E2E_TEST=0

# --- Postman MCP ---
# POSTMAN_API_KEY is required for Postman API integration. Must be a non-empty string.
POSTMAN_API_KEY=your-postman-api-key

# --- Output Directory ---
HTML_OUTPUT_DIR=output
