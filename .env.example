# =============================================================================
# Aither — Hemera Academy API Integration
# =============================================================================
# Copy this file to .env and fill in the values.
# NEVER commit .env to version control.
# =============================================================================

# --- Hemera Academy API ---
# Base URL of the Hemera API (full origin, e.g. https://hemera-academy.vercel.app).
# The client appends /api/service/* paths automatically — do NOT include a path suffix.
HEMERA_API_BASE_URL=https://hemera-academy.vercel.app

# Service token for Hemera API authentication (must be a valid JWT).
# Format: three base64url-encoded parts separated by dots (header.payload.signature).
# This should be a Clerk session token for the service user
# (aither-service@hemera-academy.com with api-client role).
# Generate via Clerk Dashboard → Backend API → Sessions.
# Example format: eyJhbGciOiJSUzI1NiJ9.eyJzdWIiOiJ1c2VyXzEyMyJ9.signature
HEMERA_SERVICE_TOKEN=your-clerk-jwt-token-here

# --- Clerk Authentication ---
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_...
CLERK_SECRET_KEY=sk_...

# --- SMTP Notifications ---
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=password
SMTP_FROM=aither@example.com
NOTIFY_EMAIL_TO=operator@example.com
NOTIFY_FAILURE_THRESHOLD=3

# --- Rollbar Error Monitoring ---
ROLLBAR_SERVER_TOKEN=your-rollbar-server-token
NEXT_PUBLIC_ROLLBAR_CLIENT_TOKEN=your-rollbar-client-token

# --- Rollbar MCP Server (read+write scope token for AI tools) ---
ROLLBAR_ACCESS_TOKEN=your-rollbar-read-write-token

# --- Rollbar Control Flags ---
# Set to 0 to disable Rollbar entirely
# NEXT_PUBLIC_ROLLBAR_ENABLED=1
# ROLLBAR_ENABLED=1
# NEXT_PUBLIC_DISABLE_ROLLBAR=0

# --- Rollbar Sampling Rates (0.0–1.0) ---
# ROLLBAR_SAMPLE_RATE_ALL=1
# ROLLBAR_SAMPLE_RATE_INFO=0.05
# ROLLBAR_SAMPLE_RATE_WARN=0.05
# ROLLBAR_SAMPLE_RATE_ERROR=1
# ROLLBAR_SAMPLE_RATE_CRITICAL=1

# --- Privacy / PII ---
# Set to 1 to allow userId/email in error reports
# NEXT_PUBLIC_TELEMETRY_CONSENT=0
# ROLLBAR_ALLOW_PII=0

# --- E2E Testing ---
# E2E_TEST=0

# --- Postman MCP ---
POSTMAN_API_KEY=your-postman-api-key

# --- Output Directory ---
HTML_OUTPUT_DIR=output

# --- Context7 (Documentation MCP) ---
# API-Key für Context7 (verwende einen Secret-Key im Format ctx7sk_...)
# Setze diesen Wert in deiner lokalen `.env` ( niemals echte Keys in commits )
CONTEXT7_API_KEY=ctx7sk_your_api_key_here
